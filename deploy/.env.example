# Variables para coturn (deploy/docker-compose.yml)
# Copia este archivo como deploy/.env y pon los valores reales.

# IP publica del VPS (obligatorio).
EXTERNAL_IP=203.0.113.10

# Dominio o nombre logico del servicio.
TURN_REALM=pikantepe

# Credenciales que usara el backend en TURN_USERNAME / TURN_CREDENTIAL.
TURN_USERNAME=pikantepe
TURN_CREDENTIAL=cambia_esto_por_algo_largo
