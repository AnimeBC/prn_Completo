import { GenericSkeleton } from '@/_Extras/Skeleton/skeleton';

// Match con alguien: hero centrado (placeholder) -> esqueleto genérico.
export default function Loading() {
  return <GenericSkeleton />;
}
