import { DetailSkeleton } from '@/_Extras/Skeleton/skeleton';

// Detalle de pack: visor grande + columna derecha (340px, como su .grid real).
export default function Loading() {
  return <DetailSkeleton variant="pack" />;
}
